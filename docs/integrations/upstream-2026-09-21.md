# Upstream integration, September 21, 2026

## Baselines and scope

- Fork baseline: `f898452f02bb03514c61f65d073d9423f0157471` (`origin/main`).
- Upstream integrated: `c00623d16ea93bbf18779f4f2b3062d956fe82ea` (`upstream/main`).
- Common ancestor: `4749527f65cf6dabab3524d8c2ea6c62a1bb4ce0`.
- Divergence at fetch: 118 fork commits, 376 upstream commits.
- Branch: `integration/upstream-2026-09-21`, created from origin/main in the existing isolated BB worktree.
- Original checkout remains on `wip/workspace-safety-before-upstream` at `480be3b55`, with 22 modified and four untracked files. Its startup, GitHub, Tasks, and generated-template edits are excluded. All other branches and worktrees are preserved.

## Integration plan

1. Merge the pinned upstream commit and reconcile conflicts by subsystem.
2. Preserve fork workspace admission, canonical identity, runtime reliability, read workers, visible/copyable history, explicit repository tracking, plugin defaults, and publishing restrictions. Prefer upstream implementations when equivalent.
3. Preserve shipped migrations, generate the combined schema with Drizzle, and test fresh and fork/upstream upgrades.
4. Advance the daemon protocol beyond both baselines, verify contracts through Turbo, exercise isolated QA, obtain independent review, and fix findings.
5. Commit, push, open a fork-main PR, and monitor checks without merging or publishing.

## Behavior reconciliation

Evidence and verification results are recorded below as checks complete. An implementation entry is not a claim that its checks passed.

| Behavior | Resolution | Implementation evidence |
| --- | --- | --- |
| Shared unmanaged workspace protection | Adapted | Canonical path identity, durable admissions, host reservations and mutation leases remain; admission reserve participates in upstream machine wake policy. |
| Runtime/process reliability | Adapted | Fork process diagnostics and idle-reap guards coexist with upstream dependent-worker cleanup and provider session ownership. |
| SQLite read workers | Adapted | Timeline and list read worker retained; new completed-turn display and detail pagination options cross its validated boundary. |
| Assistant prose and fork history | Preserved | Thread-view prose regressions retained; fork route test combines copied history with upstream explicit lifecycle ownership. |
| GitHub tracking and managed sources | Adapted | Explicit tracking retained with upstream GraphQL sweep/CLI; managed-source opt-in survives renamed plugin contracts. |
| Default plugins | Preserved | Account Pooler and Provider Usage remain enabled by default. |
| CI/publishing restrictions | Preserved | Fork publish gates retained alongside upstream CI changes. |

## Compatibility

Daemon protocol 216 is greater than upstream 215 and fork 196. The wire includes fork admission RPCs and upstream provisioning/machine changes; shared compilation alone is not considered compatibility evidence.

Both shipped migration 0115 identities are retained; the migration implementation and upgrade tests document divergent-history handling. No shipped SQL is rewritten and snapshots are generated with Drizzle.

## Verification

Pending implementation checks, independent review, isolated QA and PR CI.
